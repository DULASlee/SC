# Lessons Learned — GenCollector Harness

<!--
  格式：[来源] 场景 → 教训
  规则：每条不超过 2 行；场景必须具体到可识别触发条件；纯抽象道理无效
  维护：每个任务卡 done 时至少提炼 1 条；每周整理去重
-->

- [TASK-003] Windows Python subprocess 默认 GBK 编码 → 必须显式指定 `encoding='utf-8'`，否则 silent-pass 且门禁失效
- [TASK-003] AI 执行者用空格规避机制自检（TASK 004 vs TASK-004）→ 机制应支持 done 状态卡免检，而非靠 hack 绕过
- [TASK-004/005/006] 事后补卡不能成为常规流程 → 必须先开 PR 经架构师审核，一次性豁免需记录在 governance.md
- [TASK-004/005/006] git commit-tree 在 unborn 状态下构造测试 commit → 会损坏本地 git 历史，禁止用此方式做脚本自测
- [TASK-004/005/006] 架构师指令内部矛盾（"立即 pull" vs "先看 diff"）→ 执行者发现矛盾时应停下来报告，这是正确行为
- [TASK-004/005/006] emoji 在 PowerShell GBK 下抛 UnicodeEncodeError → hook 脚本输出只用 ASCII（[OK]/[FAIL]/[WARN]）
- [PR 4] 施工包中 done_when 写了非法 gate 格式 → 施工包作者必须先用 validate-task-card 校验自己写的卡模板
- [PR 4] 红态证据校验脚本未处理"卡为 done 状态"的跳过逻辑 → 校验脚本必须读取卡状态，done 卡不触发红态检查
- [origin sync] 本地 main 与 origin 出现分叉前未检查 diff → 执行 `git pull` 前必须先 `git fetch` + 查看 diff stat
- [harness] 任务卡 allow_write 路径写 `src/**` 通配符 → 必须用具体路径，防止执行者越权修改
- [TASK-008] 架构师豁免授权未用 hook 认可的语法（[APPROVED-BY:] 标记）→ 豁免必须同时满足：①口头/文本明确授权 ②commit message 含 `[APPROVED-BY: <name>]`。架构师签发豁免时应直接提供完整 merge 命令含标记
- [hook-design] commit-msg hook 的 protected-paths 检查与铁律豁免是两层独立机制 → 铁律豁免不等于 hook 豁免，两者需分别满足。未来考虑：hook 识别"一次性豁免"关键词自动放行，或架构师设 SKIP_PROTECTED_CHECK=1
- [parallel-sessions] 同一仓库开两个并行 AI 会话 → 分支状态不可预测、文件冲突风险。规则：同一时间只允许一个 AI 会话操作仓库
- [TASK-009/010] 施工包 Phase H 期望"无 WARN"是错配 → 文件迁移（git mv）不等于全局引用更新。迁移后 grep 必然发现历史文档中的旧路径引用，这是预期行为而非缺陷。施工包应区分"迁移完整性检查"和"引用一致性检查"，后者单独开卡
- [TASK-009/010] 历史任务卡（TASK-008.yaml）引用旧路径 → 任务卡是历史工件，不应为迁移而篡改。正确做法：保留原引用 + 加注释说明路径已变更
- [TASK-013] 并行会话在 feature 分支上留下未授权 commit → 即使内容"看起来有用"也必须 revert/reset。混入未授权提交会污染任务卡的 scope 边界，且可能复活已删除文件。检测手段：`git log` 发现非本会话 commit → 立即暂停
- [TASK-013] `git stash` + `git reset --hard` + `git stash pop` 是清理污染分支的标准操作。feature 分支的 force push 是允许的，不违反铁律 13
- [TASK-013] `git stash` 默认不保存 untracked 文件 → 重建比恢复快。重要 untracked 文件应 `git stash -u` 或先 `git add`
- [TASK-017] 任务卡 allow_write 不含 lessons-learned.md 但铁律 12 强制修改它 → 每个合规 PR 必挂 L3 scope 门禁（4 个 PR 连续红灯）；卡模板与 active 卡必须包含该文件
- [TASK-017] check-pr-scope 先查 deny 后查 allow 且卡内 allow/deny 区间重叠（TASK-012 `tests/**/*.cs`）→ allow 成死信；v3 已改为 allow 先行 + 重叠显式报 [CONFIG-CONFLICT]
- [TASK-017] scope 检查器若用"全文 TASK-\d+"提取授权卡，会把正文里引用/对比/路径中的历史卡（如 draft TASK-001 的 deny `.harness/**`）也加载进并集 → 自举 PR 自撞 10 个 CONFLICT；v3 只认交付声明（`## TASK-XXX` 标题 / Closes|Fixes|Refs|任务卡 关键词引导）+ 卡须 ready/in_progress，否定语境行（未改动/不在/not）整行跳过
- [TASK-017] DSH shell 工具全部 0xC0000142 且零输出 ≠ pwsh 未装/损坏：是 workspace-write 的 Windows ACL restricted-token 沙箱 runner 自身启动失败；danger-full-access 绕过沙箱即正常。诊断顺序：先用升级模式跑一次再下"环境坏了"结论，勿在表象层反复重试同一命令
- [TASK-017/TASK-018] 设计权衡：v3 声明式卡号提取（只认 `## TASK-XXX` 标题 / Closes|Fixes|Resolves|Refs|任务卡 引导）把 PR 模板与提交规范变成硬约束——PR 描述不按此约定声明卡号，门禁会误判越界。收益（杜绝散文卡号被误当授权卡）大于代价（模板写法受限）；任何 PR 模板改版必须同步评估该解析规则，否则半年后换写法必踩坑
- [dispatcher-mvp] 编排层"不常驻"设计：dispatch/poll 各只做一轮，状态全落盘（RUNS.jsonl 账本 + git worktree 隔离）→ 进程崩了重跑 poll.py 即可续，无需守护进程；前提是同一时刻只跑其一（账本无跨进程锁，串行即正确性保障）
- [dispatcher-mvp] 判定权零让渡：自动化脚本永不写 done，只能改 in-progress（占坑）/blocked（超重试）/awaiting-review（门禁通过）；done 唯一来源仍是人类 verify-all.py + archive-task.py。轻量机器门禁只做 scope+规模+卡校验三层，dotnet 全量 build/test 留验收侧
- [dispatcher-mvp] 机器门禁量 worktree 未提交变更必须 `git -c core.quotepath=off status --porcelain -uall`：默认会把整棵未跟踪目录折叠成一行（如 `newdir/`）→ deny_write 命中与规模上限双双失效，越界新建被判 pass
- [dispatcher-mvp] `python -m unittest discover -s .harness/tests -t .` 在 Py3.14 报 `ImportError: KeyError: ''`——顶层目录名 `.harness` 以点开头像非法包名；顶层须用 `-t .harness`（写成 runner 或文档固化，别散在各任务）

- [Phase2] `loop --once --max-tasks 0` 不是纯只读 dry-run → poll 会把 fail-exec 回收并重派新 attempt（fire-and-forget 还会拉起 dsh 子进程），跑前须确认副作用可接受，runs/worktrees 必须 gitignore
- [Phase2] 运行手册里的每个数字/语义都要先 grep 对码（预算 2000/12288、fail-fast、pre_approved 三规则皆有对应实现行）→ 文档断言无码可依即虚假文档，写前先取证

- [Phase2] Windows 下 subprocess 直拉 npm 垫片(dsh 无 exe)必 FileNotFoundError exit=-1 → run-exec 按后缀经 cmd.exe/pwsh 包裹；教训：e2e 必须含一次真 headless 派发，dry-run 永远发现不了执行器链路问题
- [TASK-021] 配置类问题先查官方 FAQ/quickstart 取 Base URL 再发测试请求 → 6 轮对话里把 apihub.agnes-ai.com/v1 当中国站 Base URL，命中"中文/国际站子域名不同"陷阱（实测对照 wiki.agnes-ai.cn/zh-Hans/docs/faqs 第 2 条才定位为 api.agnes-ai.cn/v1）
- [skill-scout] 用户口述的 skill 名若搜不到（grep.app + Bing + GitHub code search + 官方仓库 skills 目录均 0 命中）→ 立刻在官方仓库找**语义近邻**（discernment ≈ 辨析 ≈ 抗幻觉），不要为了"答点什么"硬编内容——硬编就是它要防的幻觉本身
- [skill-install] 集成第三方 skill 写入 docs/ai-workspace/skills/ 后必须 `Invoke-WebRequest` 拉 raw 源做归一化字节比对（TrimEnd LF 后逐字节 Compare-Object）→ OpenCode Write 工具在 Windows 下写 CRLF，对比脚本需 `-replace "`r`n","`n"`；trust-but-verify 不要只读自己刚写的内容
- [harness] Anthropic 官方 skill `discernment-nudge` 已落地 docs/ai-workspace/skills/discernment-nudge/ → 触发后必须严格用 "A few things worth a second look:" 引导 + 2-3 个具体到答案细节的 <120 字符反问，**每会话最多一次**；与 verification-before-completion（自验）正交，与 systematic-debugging（修 bug 根因）正交
- [skill-scout] System prompt 的 `<available_skills>` 列表**不一定包含本地所有 skill**——遇到 "我知道有这个 skill" 时直接去 `~/.config/opencode/skills/<name>/SKILL.md` 找，比靠系统 prompt 列表可靠（已验证：skill-scout 在磁盘但不在 available_skills 中）
- [skill-install] Skill 正文自报来源（"salvaged from PR #1232 by ...")必须独立验证——本次 skill-scout 自称来自 anomalyco/opencode PR #1232，实际查证是 necaris 的 Gemini 工具修复 PR，**完全无关**。规则：自报来源 → 必须 `Invoke-WebRequest` 拉 PR/commit 页确认内容匹配，否则视为"无可验证源"，写 ORIGIN.md 透明标注
- [skill-install] 无外部可验证源的 skill（无 license 字段、无可字节比对的 raw 上游）→ 项目级落地时**不写 LICENSE.txt，改写 ORIGIN.md**——记录：唯一源、自报来源验证结果、license 立场、风险等级、维护建议。许可与下载策略：默认"All rights reserved"，不二次分发
- [skill-scout] 搜未知的 skill 名不能用 `raw.githubusercontent.com/<owner>/<repo>/main/skills/<name>/SKILL.md` 直猜路径——404 ≠ 不存在；路径可能是 skills-find、skill-find 或根本不在该仓库。**正确顺序**：GitHub Repo Search API (`api.github.com/search/repositories?q=<keyword>+SKILL.md`) → 命中后用 Contents API (`/repos/<owner>/<repo>/contents/skills`) 列出真实目录 → 再拉 raw 源。本次 skill-find 走对路径的关键是 qCanoe README 字面提到 "Pair with find-skills"——提供了第三方独立证据
- [skill-install] 用户口述名 vs 真实名 ≠ 永远不同。`skill-find` 用户说 "Find skills"（复数功能），frontmatter `name` 是单数 `skill-find`；不要因"用户说复数我就搜复数"而跳过单数候选。语义近邻 ≠ 字面同名
- [skill-install] 当一个仓库的 README 描述**承诺了某个 skill 但目录里没有**（anbeime/skill 写 find-skill 但 skills/ 下不存在）→ README 与代码不同步是常态，必须用 Contents API 实查。验证 fake 本身也是 skill：原 README 第 1 句描述就足以否决
- [harness] skill-find 的前序 skill 是 **NONE**（entry-point，由用户触发短语直接调用）——不是被上游 skill dispatch 出来。这意味着 skill-find 可独立安装，**不需要配套 skill-evaluate**。本项目只装 skill-find，不连带装同 toolkit 的 5 个 meta-skill
- [skill-retire] 退役任何 skill 前**必须**做依赖审计：ripgrep 整个仓库 + 全局 skill 目录 + system prompt available_skills 列表，三处全查。本例 skill-scout 审计结果：所有引用都在自描述文件（README/ORIGIN/SKILL.md/Lessons）里，没有任何 HARNESS 路由器、hooks、配置、其他 skill 的 frontmatter `required_skills`/`Predecessor`/`depends_on` 字段依赖它——可安全退役
- [skill-install] skill-evaluate 的 Phase 3.5 lexical security 扫描**依赖** `skills/library-audit/references/security-spec.md`——独立装 skill-evaluate 而不装 library-audit 时 Phase 3.5 降级到"只标记 inline-obvious secrets"并打印 degradation 日志。本项目只 3 个 skill，library-audit 过重，先不装
- [skill-retire][2026-09-19 实战] skill-scout 退役完成。审计三处全查后确认孤儿（无任何依赖方），按用户确认选 A 立即退役。退役前后对比：删除前 4 个 skill（discernment-nudge / skill-scout / skill-find / skill-evaluate），删除后 3 个。审计报告和退役决策记录在 README.md "已退役" 段，全局副本 `~/.config/opencode/skills/skill-scout/` 保留作应急回滚源（4496 字节，byte-identical）。**退役原则**（已沉淀在 lessons 第 49 行）：任何 skill 退役前 ripgrep 仓库 + 全局 skill 目录 + system prompt available_skills 三处全查，确认无 `required_skills`/`Predecessor`/`depends_on` 字段引用
- [skill-search][2026-09-19 反思][2026-09-21 标 deprecated] 搜 skill 不要漏 `~/.minimax/skills/`——**该条目自 09-20 起因 DSH 是 plugin-driven（`@deepseek-ai/dsh-skill` + `dsh-skill-filesystem` 的 5 个默认根：`<projectRoot>/.dsh/skills`、`<projectRoot>/.agents/skills`、`customSkillDirs`、`<dshHome>/skills`、`<agentsHome>/skills`）被证伪：`~/.minimax/skills/` 不在这 5 个根里，DSH 根本不扫描该路径，是 OpenCode 时代残留，勿再当真扫描路径**。保留本条仅为记录"哪条旧教训已被哪条新证据 supersede"，新 agent 遇 DSH skill 发现/安装问题直接看 [dsh-skill] 2026-09-20 两条（第 58/59 行）
- [skill-install][2026-09-19 反思] 用户指令"写进路由规则"隐含"find-skills 已经装好"——不要再重复安装。本轮我立即下载+写到项目唯一源 `docs/ai-workspace/skills/find-skills/` 是**误装**，已删除。规则：执行任何安装动作前必须先 `Get-ChildItem` 探查 4 个候选路径（`~/.claude/skills` / `~/.config/opencode/skills` / `~/.agents/skills` / **`~/.minimax/skills`**），确认不存在才能装
- [harness] skill-find 与 find-skills 触发词高度重叠（都命中 "is there a skill for X"），用户给定的路由规则已沉淀到 `skill-find/ORIGIN.md`：问"已有什么" → skill-find（本地资产盘点）；问"想要新能力" → find-skills（远程生态采购）。下次任何 harness/agent 遇到重叠触发词，**先看 ORIGIN.md 路由表**，不擅自选边
- [dsh-skill][2026-09-20][决定性发现] DSH skill 是 **plugin-driven**，不是路径扫描。`@deepseek-ai/dsh-skill` 只是 registry 壳（lib/index.js 第 7-9 行：providers register sources with registerProvider()），实际发现由 `@deepseek-ai/dsh-skill-filesystem` 执行。fs provider 默认 5 个根（README line 33-39）：`<projectRoot>/.dsh/skills` (rank 100)、`<projectRoot>/.agents/skills` (200)、`customSkillDirs` (300)、`<dshHome>/skills` (400)、`<agentsHome>/skills` (500)。**本项目 `docs/ai-workspace/skills/` 不在这 5 个根里**→ 18 个 SKILL.md 从未进入 DSH skill registry
- [dsh-skill][2026-09-20] 修复路径不是 Junction、不是复制、不是新写 plugin——`customSkillDirs: z.array(z.string()).default([]]` 是 schema 接受的字段（src/index.ts line 84-100），在 `cordis.patch.yml` 给 `@deepseek-ai/dsh-skill-filesystem` 加 `config.customSkillDirs` 即可让 fs provider 扫任意目录。但 web-app bundle 把 base 的 host-plane `skill-filesystem` 和 `tool-skill` 都 `disabled: true` 了（`web-app/cordis.patch.yml` line 410-411），由每个 agent preset 接管（standard preset `agent.cordis.yml` line 80-87 默认挂 `skill-filesystem` 无 config）→ 正确做法是改 preset（复制 standard 到 `<dshHome>/.agent-presets/` 加 `customSkillDirs`），不是改 cordis.patch.yml
- [dsh-mcp][2026-09-20] DSH MCP 接入也是 plugin-driven。`@deepseek-ai/dsh-mcp-client` 是官方包，每个 server 一个 entry，`cordis.patch.yml` 里写 `id + name + config`。**没有 MCP auto-discovery**：磁盘上有没有二进制、有没有索引，跟能不能被调用无关。codebase-memory-mcp 装了 223MB 索引但 0 次调用的真相 = 该 MCP 没作为 plugin 加载。MCP 工具命名 `mcp__<serverName>__<tool>`
- [dsh-skill][2026-09-20] **superseded** 第 45 行（"列表不全→手动翻 `~/.config/opencode/skills/`"）：DSH skill 不读那条路径，整条 lesson 来自 OpenCode 经验不适用于 DSH。superseded 第 55 行（"搜 skill 不要漏 `~/.minimax/skills/`"）：DSH 没有那条路径，整条都是 OpenCode 残留。新 SOP：DSH 装 skill 改 `<dshHome>/.agent-presets/<name>/agent.cordis.yml` 的 `skill-filesystem` config；DSH 装 MCP 改 `cordis.patch.yml` 加 mcp-client entry；不要去翻文件系统候选目录
<!-- 新增经验追加在上方，格式保持一致 -->

- [skill-install][2026-09-19] 用 
px skills add 等外部 CLI 安装 skill 时，CLI 默认把**实体副本**写进 .agents/skills/——直接违反 AGENTS.md 唯一源铁律。正确流程：安装后立即 (1) 把实体移入 docs/ai-workspace/skills/<name>/（SKILL.md + LICENSE + ORIGIN.md，含 SHA256 字节比对）(2) 将 .agents/skills/<name> 删除并重建为 Junction 指向唯一源 (3) skills-lock.json 留在仓库根目录供 CLI 读取。坑：
px skills update 会用实体副本覆盖 Junction，更新后必须复查 LinkType。 Junction 命令：New-Item -ItemType Junction -Path .agents\skills\<name> -Target <repo>\docs\ai-workspace\skills\<name>- [TASK-024] git commit 未用 pathspec 做 index 整提交（7778f64）→ 混入前会话暂存遗留；提交前必须先 `git status --porcelain` 核对 index 残留，一律 `git commit -- <paths>` 限定提交面
- [model-config][2026-09-19] agnes-3.0-flash 的 contextWindow/maxTokens 必须以官方 wiki（wiki.agnes-ai.cn/zh-Hans/docs/agnes-30-flash.md，512K/65536）为准，不要拍脑袋写 1048576/32768，也不要抄火山引擎目录（该站无 agnes 系列）→ 虚报 2 倍上下文使 DSH compaction 永不触发 + 端点无 cache 命中，84 步×30 万 token 全量重算，简单任务跑 50+ 分钟（TTFT 占 95%）；provider 级 defaultContextWindow/defaultMaxTokens 与模型级参数重复时只留模型级
- [model-config][2026-09-19] MiniMax-M3 官方 Messages API：上下文 1,000,000（十进制，非 1048576）、max_tokens 推荐 131072/上限 524288、M2.7 上下文 204800/推荐 65536；thinking 仅 toggle（adaptive/disabled），无 high 档 → DSH 侧曾写 `reasoning: high` + maxTokens 500000 均无依据已删；端点 api.minimaxi.com/anthropic 实测可用 CN 订阅 Key（cache 命中正常），官方中国站文档另给 api.minimax.cn/anthropic，两者等价
- [model-config][2026-09-19] M3 多端核查结论：Qoder 配置在 state.vscdb `aicoding.customModels`（byokTypeKey=cp 内置模板，1000000/vl/reasoning 全对无需改）；WorkBuddy/CodeBuddy 在 `~/.codebuddy(cn)/models.json`（明文 key！），M2.x 条目曾误标 `supportsImages: true` 且 M2.5-highspeed URL 误写完整路径 → 官方明确 **M2.x 系列不支持图片/视频输入**，误标会让发图必 400；`api.minimaxi.com/v1` 与 `api.minimax.cn/v1` 经 /v1/models 实测均接受 CN sk-cp key（200），域名二选一皆可
- [model-config][2026-09-20] DSH 压缩触发点 = `floor(路由上下文窗口 × thresholdRatio)`，默认 0.8（dsh-compaction-basic `DEFAULT_THRESHOLD_RATIO`）→ 大窗口模型（agnes 512K）会长到 41.9 万 token 才压缩，遇上慢预填充端点即灾难（实测 prompt 33-44 万、TTFT 均值 69s/步）；正规调法：在 `~/.dsh/profiles/<p>/cordis.patch.yml` 按 id 覆盖 `compaction-basic` 的 `modelPolicies`（补丁**整体替换 config 不合并**；`retainTokens` 必须 < 解析阈值；与 `retainRatio` 互斥）
- [model-config][2026-09-20] 改 profile 补丁后必须双重验证：① 结构校验（`floor(window×ratio)` > retainTokens、互斥项）② `dsh --profile <p> --help` 加载校验（配置错误 fail-fast，且该模式不绑定服务）→ 两者均通过方可重启；回滚 = 恢复 `[]` 或备份文件。工具结果修剪器 `tool-result-pruner` 仅在压缩触发后运行，不能替代源头控制工具输出体积
- [model-config][2026-09-20] 怀疑"端点被路由绕道"用三步证伪：① Find-NetRoute 看真实出口网卡（本例全部公网流量走 sing-tun utun12 全局隧道，traceroute 任意目标皆"1 跳 <1ms"）② 同隧道下与海外目标比 TCP/TLS（agnes 26/74ms vs anthropic 40/944ms → 未出国）③ 上传隔离实验（大 body + 无效 model 令服务端秒拒，单独量上传：1MB 仅 0.34s）→ 三者全排除后，大请求的 10.7s 只能归服务端 prefill，勿把本地代理当替罪羊
- [model-config][2026-09-20] 判"端点缓存是否生效"禁用相邻两次同 payload 的 A/B：agnes 缓存为**异步写入**，间隔 <5s 会双 miss 而被误判"无缓存"（本次 v1-v3 报告即栽在此）→ 必须间隔 ≥10s 复测并核对 usage.prompt_tokens_details.cached_tokens；实测命中时 33 万 tok 仅 18.4s，未命中时同量 100-176s（未缓存预填充 ~2.2K tok/s）
- [model-config][2026-09-20] DSH 会话 prompt 达 32-44 万 tok/步（中位 32.9 万），构成 92.8% 来自工具结果（230 条 = 189 次调用 + 41 条修剪副本；单条最大 5 万字符），系统提示+26 工具仅 8-14K、人类输入 2.0% → "提示词不大却爆上下文"的元凶是工具输出；控制大文件/命令输出入上下文比换端点更治本，且 34% 的步整段缓存未命中无法用间隔/prune/todo 解释（端点侧问题，已反馈）
- [tool-hygiene][2026-09-20] 会话 `tool/result` 条数 ≠ 工具调用数：230 条 = 189 次真实调用 + 41 条 `tool-result-pruner` 重写副本（每次 prune 追加一条替换结果，故 `call + prune = result` 可自校验）；体积主导项是 `read`：126 次调用/123 个不同文件/占工具输出 74.6%，而 `grep` 仅 4 次 → "上下文爆炸"的成因是**用整文件读取代替检索**，不是重复读取（重复仅 3.0%，123 文件/126 次）
- [tool-hygiene][2026-09-20] 最贵的工具输出是目录递归列举：4 条 `Get-ChildItem -Recurse` 各被截断在 5 万字符 = 20 万字符（全部工具输出的 15.7%），8 条 >2 万字符的结果占 25.2%；且 46.8% 的 read 在工作区外（F:\maiyata 40、`~/.claude` 17、AppData 2）→ 目录列举改用 glob 计数、跨仓库/外部配置目录读取需显式授权、单轮工具输出设预算（≤100K 字符）
- [tool-hygiene][2026-09-20] pruner 是事后补救而非解法：54% 的发出内容最终被修剪，但在被剪前它已参与每一步 prompt 并破坏缓存前缀；且 41 次 prune 的触发点是**会话 resume 时的主动压缩**（turn 10 被 interrupted → resume → seq 700-780 连续 prune），当时窗口 1048576 阈值 83.9 万，并非压力触发 → 控制必须在**发出时**，不能依赖修剪兜底
- [model-config][2026-09-20] DSH token meter 的 4 字符/token 启发式对 CJK/代码偏低：实测 1,374,372 字符 = 438,541 真实 token（3.1 字符/token），即**真实 prompt ≈ meter 估算 × 1.28** → 用 `thresholdRatio` 调压缩阈值时必须按真实 token 折算（要真实 105K 应取 0.15 而非 0.2）；会话期间窗口还被改过 3 次（262144 → 1048576 → 524288），虚报期阈值高达 83.9 万
- [search-tooling][2026-09-20] "上下文被工具输出撑爆"的**根因是检索不精确，不是提示词太长**：本会话 126 次 read 中 47 次（413,998 字符）是文档检索，而 4 次 grep 有 2 次近乎零命中（16/128 字符）；核查发现 agent 搜的 `云端聚合`/`点丢失` 在语料中 **0 命中**（`断开`=32、`MQTT`=717）→ 纯正则跨不过"提问用词 ≠ 文档用词"，且失败时不给可行动信息，agent 只能退化为整文件读取
- [search-tooling][2026-09-20] 已交付 `docs/ai-workspace/tools/jqkj-search/`（`jqkj_search.py`+`jqkj-search.cmd`；索引 `.tools/search/index.db` 为构建产物、已被 .gitignore 覆盖）：SQLite FTS5(trigram) 索引 268 文件/2,049 章节/804 符号（建索引 1.5s），四命令 `doc`(章节级+行号)/`sym`(定义+引用)/`outline`(结构)/`find`(路径计数)；实测文档检索子任务 413,998→8,500 字符（**48.7×**），`sym` 157 字符替代 5,988 字符文件读取 → 用「小输出高精度检索」替代「整文件读取+目录递归列举」
- [search-tooling][2026-09-20] 检索工具实现三坑：① FTS5 **trigram 分词器无法匹配 <3 字符的词**（CJK 双字词"断开"必失配）→ 需 ≥3 字符走 FTS、短词用 LIKE 兜底；② 零命中必须回**语料中真实存在的 2 字片段**（`点丢失`→`丢失(44)`），否则 agent 只能放弃转整文件读取；③ 排序用**命中密度**（命中数/千字符）而非命中数，否则数百行巨型章节霸榜；另需内容 sha1 去重并排除 `.harness/worktrees/`（同一仓库多任务态副本会造成 3-4 条重复命中）
- [cache][2026-09-20] agnes 端点的**缓存命中率与 prompt 规模无关**（r(prompt, miss) = **-0.035**，分桶非单调），因此"提高压缩阈值以增强缓存率"**不成立**：实测 miss 率 41% 由**后端不一致**造成——同一 turn、间隔 <5s、前缀完全相同，一部分后端返回 `cacheRead=383,488/392,448`（缓存整个会话前缀），另一部分只返回 `6,400/7,936`（仅共享系统+工具块）；miss 状态**黏性** P(miss|前一步 miss)=63.4% vs P(miss|前一步 hit)=24.1%。阈值决策见 ADR-0005（保持 0.2，恢复 0.7 会多花 3,704s/100 步）
- [cache][2026-09-20] 排查缓存 miss 必须先证伪"间隔/TTL"与"事件触发"：实测间隔 <5s 的 88 步仍有 37.5% miss，`llm/retry` 相关 -1.1pt（无关）、`todo/write` -20.6pt（负相关）、turn 起始 +29.2pt、>10min 间隔 87.5%（仅覆盖 8 步）；**唯一确认有害的前缀改写是压缩/prune 爆发**（`cacheRead` 435,968 → 6,400，随后 3 步连续全 miss）。归因前务必算转移矩阵，**黏性(63%)与交替(<30%)指向完全不同的根因**（粘性=后端状态，交替=轮询路由）
- [latency][2026-09-20] 延迟优化的**收益上界必须写清**：当 41% 的 miss 由端点决定时，即使 prompt 降到 0，100 步 TTFT 地板仍是 **2,150s**（100 × 21.9s 命中开销）——任何"降到 1,200s"的承诺都是不可能达成的。反事实模型要用实测值校准（本模型对 0.8 档预测 7,813s vs 实测 6,896s，误差 13%）后再用来比较方案，不要直接用直觉倍数
- [windows][2026-09-20] `.cmd`/`.bat` 包装器**必须纯 ASCII**：中文注释在 GBK 控制台下被拆成乱码并被当作命令执行（实测 stderr 报 `'MQTT' is not recognized as an internal or external command`）；用法说明写进 README，脚本内只用英文注释。同理，Python 脚本在 Windows 控制台输出中文须显式 `sys.stdout.reconfigure(encoding='utf-8')`，否则乱码
- [skills-mcp][2026-09-21][部分验证] 项目 skills/MCP"模型识别不了"的三条断点：① DSH 侧靠 Junction 通到 `C:\Users\admin\.dsh\skills`（本会话 23 个 skill 在 `<available_skills>` 中可见，已通）② 派发器 `.harness/dispatch.yaml` 的 `skills_dir` 原值指向 Claude 私有副本 `~/.claude/skills/superpowers`（违反"AI 配置唯一源在 docs/ai-workspace/"原则，该副本下 junction 目标还缺 SKILL.md）→ 已改指 `F:/JQKJ/docs/ai-workspace/skills`；另 `skills.py::_candidate_paths` 只剥 `superpowers/` 前缀不补场景子目录候选，源目录按场景组织（superpowers/<name>）时裸名 MISS 静默跳过 → 已补 `base/superpowers/<name>/SKILL.md` 候选，**并封住"最具体优先"的灾难边界**：候选命中后做内容健全性校验（`_skill_md_plausible`：frontmatter/H1/`## ` 三选一），校验失败降级到次选候选（场景子目录/裸目录），全候选内容异常才按 MISS 跳过并向 stderr 打 `[skills] CONTENT-BAD`——优先级越高"错得越坚定"（命中错的那个而非没命中），内容校验是"命中错的那一个"的唯一防线。诊断行统一走 `_diag`（UTF-8 强制 reconfigure + ASCII 兜底），stdout 契约永远干净（`test_diagnostics_never_on_stdout` 固化）。`python .harness\tests\test_skills.py` 13/13 绿（前缀命中/裸目录优先/场景回退/坏内容降级/全坏 MISS/stdout 纯净 各 1 条）③ MCP 侧无任何 profile 挂载 `dsh-mcp-client`（与 09-20 根因记录一致），已补 headless `codebase-memory` entry（`npx -y codebase-memory-mcp`，`failOnStartupError: true` 启动失败必须 fail-fast，不留沉默失败；本机实测 `npx -y codebase-memory-mcp --version` → 0.11.0 正常拉起，故设 true）。jqkj-search 尚无 MCP 入口（真实障碍是缺 server 封装层，非加子命令），启用步骤 + 封装层入口两条自检要求（依赖自检显式报错退出 + 启动 ready 信号向 DSH 报告）写在 mcp/README.md。**验证状态**：skill 侧改动即时生效、13/13 回归绿（已验证）；MCP 侧仅完成 YAML 解析 + npm 拉包 + `--version` 验证（`--version` 只证明包能拉/二进制能起，**不**证明 stdio 协议握手/工具注册/schema 暴露——三层判定见 mcp/README "连通性验证"段），**尚未端到端调通 `mcp__codebase-memory__get_graph_schema`**（cordis.patch.yml `patchReload: startup`，需重启 dsh 会话后在模型侧实际调用一次确认返回合法 schema，三层全通才算修通）
- [silent-failure-sweep][2026-09-21] 扫 harness 全链路找"启动时一次性决定成败、失败后静默降级"的残留点，逐环节判定：① `pipeline.py`/`poll.py`/`dispatch.py` 大量 `except ... pass/continue` 是**设计内隔离**（单卡坏不崩整轮/单 owner 坏按无 running 处理），有 `_warn_store`/`[WARN]` 兜底、留痕，**不算沉默失败**，勿"顺手加日志"破坏隔离语义 ② `modelswap.py` 读坏只告警返回空（`_warn_store`）有留痕 ③ **真残留点：`run-exec.py` 的 `except OSError` 落盘 exitcode=-1 但包装器自身 stdout 只打 `[OK]`，缺命令/文件缺失时"为什么 rc=-1"要翻 stderr.log 才知道** → 已补 `[FAIL] run-exec: OSError ... (rc 落盘为 -1)` 到 stderr（包装器自身退出码恒 0 契约不变，任务失败仍只认 exitcode.txt，poll 侧行为零变化，只是排查第一现场留痕）④ `canary.py` fail-closed（缺文件/坏文件即变更）是安全方向，无需改 ⑤ 结论：**"失败不显眼"是本次修掉的几类问题（静默跳过/启动不报错/CLI 冒充 MCP/教训写成定论）的共同根因，防复发靠"每个环节都有显式失败信号"而非再加测试**——skill 读不到报、MCP 起不来报（failOnStartupError:true + 三层判定）、依赖缺了报（封装层入口自检）、教训未验证标注（[部分验证]）。本轮只改 run-exec 一处（最小可操作修复），其余静默点已登记，是否继续加固由架构师定夺
- [cache][2026-09-20] `usage.prompt_tokens_details.cached_tokens` **不可单独作判据**：实测 agnes 112,085 tok 的请求 30.8s 完成（3,639 tok/s，快于其自身冷启动回归 2,244 tok/s 的 1.6 倍）却报 `cached=0`，同 payload 另一次用 214.1s 也报 0 → 判 miss 必须用双判据「`cacheRead<20K` **且** `TTFT ≥ max(30s, 0.5×prompt/2244)`」；两口径差值就是"命中未上报"量，要单独跟踪（本会话 ADR 口径 41% → 真 miss 35%，41% 是上界）
- [cache][2026-09-20] **真 miss 的主导可控变量是会话龄期，不是 prompt 规模**：距上次前缀改写 >60min 的 67 步真 miss 45%，<10min 的 23 步 11–20%，会话首 13 步（无 prune）0%；而同一 header 世代内 r(prompt, 真miss)=+0.039（复现 ADR-0005 证据 1）→ 长任务应**切分会话（≤40 步或 ≤60 分钟）**，继续下调压缩阈值治的是错的病（寻优 0.05 档收益 −34% 全部来自"给不可控 miss 买保险"，miss 修到 0 时只剩 −0.8%）
- [cache][2026-09-20] 「保持前缀稳定」的收益被高估：header 世代切换与 41 次 prune 爆发后 60s 内，按 `cacheRead` 判 80% 是 miss、按墙钟只有 20%（≈基线）→ 多数是**计量假象**而非真重算；前缀卫生仍要做（端点缓存实测为严格链式：命中步 `cached(k)≈prompt(k-1)` 失配中位 −0.55%，且 `cacheRead` 恒为 256 整数倍），但不能拿它当命中率问题的主解
- [cache][2026-09-20] 端点前是否隔着 Cloudflare 决定"客户端自救"是否可能：`Server: cloudflare` + `CF-RAY *-CZX`，同一 TCP 连接内 CF-RAY 尾部节点标识恒定（97d3/4e92/4e94）、换连接即变，且 `api.agnes-ai.cn` 只解析出 1 条 anycast A 记录 → **能钉住边缘节点、钉不住源站实例**（同连接同 payload 间隔 45s 复测仍 `cached=0`）；hosts 固定/换 IP/keep-alive 三条自救路全部不存在，ADR 里"缓存亲和客户端不可控"由此从推测升级为实测，只剩"请厂商提供 origin 实例头或账号级粘性"一条
- [endpoint-selection][2026-09-20] 端点选型对比必须带**阳性对照 arm**，否则"全 miss"分不清是端点不缓存还是排队：同协议下 MiniMax-M3 warm 2.1s / probe 1.3s 且 `cache_read=24,470` 完整上报，agnes 22/22 报 0 且同 payload 墙钟 1.60s/6.26s/16.76s 抖动 10×（110K 还出现 >300s 读超时）→ 无对照组时禁止下"缓存无效"结论；另 `ark-code-latest` 经 `/api/plan/v3/chat/completions` 裸调被拒 `UnsupportedModel: does not support the agent plan feature`，故 v4 记的"doubao 38 万 tok/步 4.6s"须在 DSH 实际请求形态下重测，暂不作选型依据
- [method][2026-09-20] 用户提"能不能自己在 IDE 里微调模型"时，先分层再回答：命中率因果链是「token 前缀逐字相同 → 网关分发到哪台实例 → 该实例是否还留着这条目」，**权重不参与任何一步**（微调只让 KV 池重建，命中率只会更差），且闭源 API 要用官方文档端点清单证明无训练产品（`wiki.agnes-ai.com/llms.txt` 26 页只有 chat/responses/messages + 定价/FAQ）；然后把"能改的"落到实测有数的三档：端点路由（每步固定税 22.2s vs MiniMax 2.1s ≈10.6×）、会话龄期、步数（固定税占 100 步 TTFT 的 32%，减步数是唯一手段）
- [tooling][2026-09-20] 取证脚本必须落进 `docs/ai-workspace/tools/<name>/` 并显式规定输出路径：`cache_probe.py` 初版把明细写到 cwd，在仓库根留下 `cache_probe_rows.json` → 已改为 `--out` 默认 `%TEMP%` 并清理残留；探针类工具一律带 README 写清"判读顺序 + 踩过的坑"，否则下次会重犯（本轮就重犯了一次：用相邻两次同 payload 判缓存）
- [汇报][2026-09-20] 对话汇报里写"成立（modelswap.py:47-64 _write_doc …）"= 零信息，人无法核对只能选择信或不信 → 已立铁律 16（正文零技术名词、结论前置、一次只提一个待拍板问题），根因与上次"手册数字未对码"同源：有凭据就把凭据当交付物；入库文档照旧写全细节，只改呈现位置
- [plan-review][2026-09-20] 评审实施计划必须先对码取证再下结论：并行方案 6 断言中 2 失实（账本其实已集中于主树 .harness/runs、模型配置写入点是用户级 ~/.dsh/settings.yaml 而非仓库级）+ 1 未证实（DSH 会话级模型覆盖能力是否存在）→ Formal Plan 冻结前须把每条断言对到实现行；方案自带的 P0 章节不能替代评审者独立取证
- [model-override][2026-09-20] DSH 会话级模型隔离已探针实证可行：`dsh --patch <p.yml>` 把 settings 条目重定向到 per-attempt 设置副本（config.path + watch:false；值优先级 文件层>组合层，dsh-settings mergeLayers 顺序），全局 settings.yaml 零写入（SHA256 前后一致）→ 派发侧模型切换可纯配置实现，modelswap 的 swap/restore/锁链可退役；探针顺带实测 openrouter `deepseek/deepseek-v4-flash-0731:free` 免费档已上游 404 下线（dispatch.yaml 默认 model 行失效，待架构师改）
- [eol-landmine][2026-09-20] CRLF 入仓的 blob × `.gitattributes` 显式 `eol=lf` 规则 = 该文件在**每个新 worktree 永久幻影脏**（status/diff 按属性先清洗工作区再比对 CRLF blob，checkout 修不好，只有 `git add --renormalize` 重规范化才行；`text=auto` 靠 safe-crlf 豁免躲过）→ 并行门禁（porcelain 算 scope/规模）会被幻影毒化成全量 fail-scope，施工前必须重规范化 + 逐文件字节级"仅剥 CR 零内容差"证明后单独特地提交
- [card-authoring][2026-09-20] `validate-task-card` 强制 acceptance_tests ∈ `tests/` 或 `*.Tests/` → harness Python 卡按 TASK-022/023 先例：填 ArchitectureTests.csproj 形式占位，实质验收（python 测试+红态目录）写进 notes 与 allow_write；不要给此类卡挂 `skills: [test-driven-development]`——证据门禁会拿占位 csproj 比对，必判 fail-skill

