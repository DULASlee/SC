# 项目编程铁律

本文件为项目强制执行的工程规范，所有参与者必须遵守。

---

## 1. 总原则：必须遵守

违反本铁律的任何条款必须经人类明确批准，并留下 ADR 记录。

---

## 2. 契约优先原则

> **OpenAPI、AsyncAPI、MQTT Topic、data-models、alarm-state-machine 未冻结前，禁止写 BFF、安灯服务、规则引擎、采集器业务代码。**

- "未冻结"指尚未经过正式评审并纳入版本控制的状态
- 违反此条的业务代码不允许合入主干分支

---

## 3. 禁止自行宣布完成

交付物状态只能报告为以下三种，并附证据：

| 状态 | 含义 | 证据要求 |
|------|------|----------|
| **完成** | 全部验收标准通过 | 指向通过的全部测试用例、lint 通过记录、CI 日志 commit hash |
| **部分完成** | 核心功能通过，但存在已知缺陷或缺失项 | 列出所有已知缺陷，明确哪些阻塞上线 |
| **阻塞** | 因外部依赖或不可抗力无法继续 | 提供阻塞点证据（依赖方回复、issue 链接、错误截图等） |

禁止使用"基本完成"、"差不多"、"90% 完成"等模糊表述。

---

## 4. 禁止删除测试掩盖问题

任何测试删除都必须满足以下全部条件：

1. **原因说明**：明确为何该测试不再适用或重复
2. **影响分析**：该测试覆盖的逻辑分支是否有其他测试覆盖
3. **替代方案**：是否有等价或更强的验证手段
4. **人类确认**：获得人类明确批准后方可删除

**GBK/GB18030 相关测试**（编码兼容性测试）必须保留或补回，不得以"不常用"为由删除。

---

## 5. 禁止静默回退

| 场景 | 禁止行为 | 要求 |
|------|----------|------|
| JSON 配置损坏 | 静默回退到 INI | 必须报错退出或使用安全的默认值并记录警告 |
| 敏感信息缺失 | 继续启动 | 必须报错退出，明确指出缺失的哪个字段 |

静默回退指系统在未通知用户的情况下用备用方案替代原有配置。

---

## 6. 禁止跨文档字段漂移

同一业务字段在以下所有文档/Schema 中必须**同名、同类型、同枚举值**：

- OpenAPI (openapi.yaml)
- AsyncAPI (asyncapi.yaml)
- MQTT Topic 定义
- data-models（数据模型定义）
- alarm-state-machine（告警状态机定义）

发现不一致时，必须提出 Issue 并修复，不得视而不见。

---

## 7. 所有交付物必须入 Git

以下类型文件必须纳入版本控制：

- 源代码和业务逻辑代码
- 文档（README、API 文档、设计文档）
- ADR（架构决策记录）
- 测试报告和 CI 日志
- Schema 文件（OpenAPI、AsyncAPI、JSON Schema）
- 样本配置文件（不含真实敏感信息）

---

## 8. 所有构建必须固定平台目标

| 项目/组件 | 平台目标 | 说明 |
|-----------|----------|------|
| GenCollector | x86 | 显式指定 `--arch x86` |
| 所有测试项目 | x86 | 显式指定 `--arch x86` |
| CI 构建 | `win-x86 --self-contained` | 显式指定运行时和自包含模式 |

禁止发布未指定平台目标的构建产物。

---

## 9. 所有敏感信息不得明文

密码、Token、证书、私钥等敏感信息必须通过以下方式注入：

- 环境变量：`${ENV_VAR}` 占位符
- Secret 管理工具（Azure Key Vault、GitHub Secrets 等）

禁止在代码仓库中明文存储任何敏感信息，包括：
- 注释中的示例密码/Token
- sample/config 文件中的真实凭证
- 硬编码的连接字符串

---

## 10. 所有决策必须留 ADR

以下决策必须创建 ADR（Architecture Decision Record）并纳入版本控制：

- 技术栈选型（如 Avalonia UI 框架）
- 架构决策（如 BFF 模式）
- 平台决策（如 x86 隔离）
- 回退策略（如 INI 回退机制）
- 契约统一决策（OpenAPI/AsyncAPI/MQTT 字段统一）

ADR 格式要求：放在 `docs/adr/` 目录下，包含背景、决策、后果三个核心部分。

---

## 11. 所有报告必须可验证

所有交付报告必须包含以下可验证要素：

| 要素 | 要求 |
|------|------|
| 文件路径 | 指向实际文件，路径必须正确 |
| 命令 | 提供可执行的完整命令 |
| 输出摘要 | 关键输出行（尤其是成功/失败的判断依据） |
| commit hash | 关联到具体 commit，支持回溯 |
| 测试结果 | 通过/失败数量，失败用例必须列出 |

禁止提供无法验证的结论性报告。

> **适用范围限定（2026-09-20）**：本条针对**入库交付物**（`docs/reports/` 工作汇报、`docs/testing/` 证据、验收记录等）。对话内的**工作收尾汇报**遵循铁律 16，正文不写代码坐标；两者不冲突——铁律 16 要求以"我核对了哪一处、看到了什么现象"的方式满足可验证性，结论必须能在本条要求的入库证据中找到对应物，禁止用自然语言掩盖无证据的结论。

---

## 12. 经验沉淀

**铁律 12「经验沉淀」**：每个任务卡完成（状态变为 done）时，执行者必须提炼至少 1 条经验写入 `docs/ai-workspace/rules/lessons-learned.md`。格式：`[TASK-XXX] 场景 → 教训`，每条不超过 2 行。架构师审核 PR 时检查该文件是否有新增。连续 3 个卡无经验沉淀，该执行者后续卡需额外提交"经验自查说明"。每周由执行者整理一次，合并重复项、归档已修复项。

---

## 13. 禁止直接合入 main

**铁律 13「禁止直接合入 main」**：所有变更必须通过 feature 分支 + PR + 架构师审核后 merge 到 main。禁止 `git push origin main`。

---

## 14. 文档与配置集中化

**铁律 14「文档与配置集中化」**：
1. 所有非编译产物（文档、配置、证据、脚本）必须存放在 `docs/` 对应子目录下，禁止散落在项目根或其他任意位置。
2. 所有 AI 工具配置（rules、mdc、skills、hooks、MCP）的唯一源位于 `docs/ai-workspace/`，禁止在各 IDE 私有目录维护独立副本。
3. 架构文档必须存于 `docs/architecture/`，部署文档必须存于 `docs/deployment/`，工作汇报必须存于 `docs/reports/`。
4. 测试证据和红态文件必须存于 `docs/testing/red/TASK-XXX/`。
5. 违反本铁律的文件，架构师有权要求立即迁移，相关 PR 必须包含迁移操作。

---

## 15. 执行器链路 canary

任何改动 executor_argv（含命令构造方式、垫片、包裹层，不只参数值）、更换执行机器、更换 Node 大版本后，必须先完成一次 alive-probe 级真 headless 派发并留证（RUNS 记录 + 真实 exit code），方可恢复常规 dispatch。probe 未过，禁止 dispatch。

---

## 16. 收尾汇报用自然语言（禁止技术名词堆叠）

**适用场景**：对话中向人做的进度汇报与工作收尾汇报。不适用：入库交付文档、测试证据、任务卡、ADR —— 那些照旧写全技术细节。

人不做代码核对，技术名词对人没有信息量。判断标准：**不懂编程的人读完，能复述出"结论是什么、为什么、要我决定什么"**，即为合格。

| 要求 | 做法 |
|------|------|
| 正文零坐标 | 不出现文件名、函数名、类名、行号、参数名、字段名、路径、命令、缩写。一律翻译成"什么东西会怎样、对业务意味着什么" |
| 结论前置 | 开头一句话结论 + 最多三条"会改变决策"的点，其余下移 |
| 一次一个问题 | 待拍板项只留最关键的 1 个，多个问题等于没有问题；其余自己拿主意并承担可回退风险 |
| 单一论点 | 同一判断只出现一次，删除"换个说法再讲一遍"的段落 |
| 表达证据 | 说"我核对了哪一处、看到了什么现象"，不说位置编号；禁止用自然语言包装无据结论 |
| 篇幅上限 | 正文不超过证据部分的 1/3；写不满说明没做判断，只在做搬运 |

**反例（真实违规记录）**：`成立（modelswap.py:47-64 _write_doc、175-194 swap_for_run 备份 + _prune_backups）`
**正例**：`这个能力项目里已经有了——它改配置前会先存一份旧的、出错能退回。方案说照它做一份，我核对过，确实能复用，不用新写。`

**例外**：人明确索要技术细节时另起一段给出，不混入正文。默认不在正文附技术清单。

**与既有铁律的关系**：铁律 11（报告可验证）管入库证据，本条管对话表达，取证照做，过程不外露。本仓 `[Phase2]` 已有教训"文档断言无码可依即虚假文档" —— 本条不放松该要求，只改变它的呈现位置。

**违规处理**：按本文末尾"违规处理"执行；人反馈"看不懂 / 太琐碎 / 全是术语"即视为本条违规，不得辩解为表述偏好问题。

---

## 违规处理

违反本铁律：
1. 首次违反 → 口头/书面警告，要求修复
2. 重复违反 → 阻塞合入，直到修复并 Review 通过
3. 严重违反（如明文存储敏感信息） → 立即修复并通报

