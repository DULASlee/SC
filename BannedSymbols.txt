# L2-A BannedSymbols —— 一行消灭一整类错（编译期拦截）。
# 加载：Directory.Build.props 引 Roslynator.Analyzers + 本文件。
# 注：Roslynator 默认规则集不直接解析 BannedSymbols.txt；
#     本文件被解析后由编译器层面（符号扫描）覆盖，详见 .ci/l2-a-mechanism.md。
# 错误码：RS0030 / 编译器级诊断。

# 错误类：使用平台默认编码 → 隐式 GBK / Shift-JIS 混入，跨平台炸
P:System.Text.Encoding.Default;使用显式编码，如 Encoding.UTF8

# 错误类：未注入时钟 → 测试不可重放、时区漂移
P:System.DateTime.Now;使用 IClock 注入或 DateTime.UtcNow
P:System.DateTime.UtcNow;使用 IClock 注入，避免时钟回拨导致数据错乱

# 错误类：同步 sleep 阻塞线程池 → 吞吐断崖
M:System.Threading.Thread.Sleep(System.Int32);使用 Task.Delay
